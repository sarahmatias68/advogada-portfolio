/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['placeholder.com'], // Adicione aqui os domínios das imagens que você vai usar
  },
}

module.exports = nextConfig
